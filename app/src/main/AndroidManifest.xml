<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Location permissions: required for SOS location acquisition -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".utils.WomenSafetyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:theme="@style/Theme.WomenSafetyApp"
        android:supportsRtl="true">

        <activity
            android:name=".ui.common.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.WomenSafetyApp.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.common.WelcomeActivity" android:exported="false" />
        <activity android:name=".ui.common.RoleSelectionActivity" android:exported="false" />
        <activity android:name=".ui.auth.AuthActivity" android:exported="false" />

        <activity android:name=".ui.woman.WomanMainActivity" android:exported="false" />
        <activity android:name=".ui.guardian.GuardianMainActivity" android:exported="false" />
        <activity android:name=".ui.police.PoliceMainActivity" android:exported="false" />

        <activity android:name=".ui.common.EmergencyActivity" android:exported="false" />
        <activity android:name=".ui.common.LocationActivity" android:exported="false" />

    </application>

</manifest>